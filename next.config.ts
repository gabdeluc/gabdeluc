import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* In Next.js 16, turbo is either top-level or handled via CLI.
     Removing the experimental block fixes the TS error.
  */
};

export default nextConfig;